@model IEnumerable<ResultManagementSystem.Models.ClassInfo>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClassInfoName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Class_.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Section.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClassInfoName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Class_.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Section.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClassInfoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ClassInfoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ClassInfoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
